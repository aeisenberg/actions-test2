name: "Code Scanning - CodeQL"

on:
  pull_request:

jobs:
  codeql:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@aeisenberg/multi-init
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/init@aeisenberg/multi-init
